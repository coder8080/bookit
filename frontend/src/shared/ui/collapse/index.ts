import Root, { CollapseRootProps as RootProps } from "./Collapse";
import { createCollapseTransition } from "./transition";

export type { RootProps };

export { createCollapseTransition, Root };

const Collapse = Root;

export default Collapse;
