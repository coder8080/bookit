import Root, {
  SelectRootBaseProps as RootBaseProps,
  SelectRootOption as RootOption,
  SelectRootProps as RootProps,
  SelectRootVariantProps as RootVariantProps,
} from "./Root";
import styles from "./styles";

export type { RootBaseProps, RootOption, RootProps, RootVariantProps };

export { Root, styles };

const Select = Root;

export default Select;
