import Root, {
  AvatarRootBaseProps as RootBaseProps,
  AvatarRootProps as RootProps,
  AvatarRootRenderProps as RootRenderProps,
  AvatarRootVariantProps as RootVariantProps,
} from "./Root";
import styles from "./styles";

export type { RootBaseProps, RootProps, RootRenderProps, RootVariantProps };

export { Root, styles };

const Avatar = Root;

export default Avatar;
