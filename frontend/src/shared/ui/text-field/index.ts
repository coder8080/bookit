import Root, {
  TextFieldRootBaseProps as RootBaseProps,
  TextFieldRootProps as RootProps,
  TextFieldRootVariantProps as RootVariantProps,
} from "./Root";
import styles from "./styles";

export type { RootBaseProps, RootProps, RootVariantProps };

export { Root, styles };

const TextField = Root;

export default TextField;
